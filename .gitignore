# Swift Package Manager
.build/
.swiftpm/
# Package.resolved ist bewusst eingecheckt: der Notarisierungs-Broker baut mit
# `--only-use-versions-from-resolved-file` und vergleicht die Datei Byte für
# Byte mit seiner eigenen, geprüften Kopie (siehe README, „Veröffentlichen").

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
DerivedData/
build/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift
*.o
timeline.xctimeline
playground.xcworkspace

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Local, machine specific configuration (never commit a real user config)
openzonr.local.json
